Redesign my website using the interaction + motion principles shown in my reference videos (circular “lens” swipe navigation and About chapter scrolling). DO NOT copy ASM Mobius content or layout 1:1; recreate the behavior and premium feel with an original design tailored to my site’s content.

MUST MATCH THESE BEHAVIORS:

WORKS (Focus mode): full-screen project viewer with a large circular lens in the center. Outside the lens is vignetted + subtle blur + liquid-glass distortion. Inside lens is clearer. Center the project title + subtitle in the lens. Add PREV and NEXT labels with long thin rules left and right. Add bottom-right counter “current — total”.

Interactions: Users can click/drag the lens horizontally (desktop) or swipe (mobile). Drag should reveal next/prev project image with a watery displacement transition. Release past a threshold commits to next/prev; otherwise snap back.

ABOUT: cinematic editorial page with language toggles (En/Cn/Jp), an About label, and a chapter progress indicator “01 — 05”. Use smooth scroll with snap-to-section easing; sections include giant typography and occasional image moments.

ORIGINALITY RULES:

Use ONLY my images/icons from /public/assets (create placeholders if missing but keep paths ready).

No copying of ASM Mobius wording or imagery.

Typography/spacing/motion should feel “museum editorial + cinematic tech” and super premium.

TECH REQUIREMENTS:

Build in React (Vite or Next) with clean components.

Implement the lens swipe transition with WebGL (Three.js or a lightweight shader plane) using a displacement/noise map for the watery distortion.

Provide a graceful fallback (CSS clip-path + blur) if WebGL not supported, and respect prefers-reduced-motion.

Mobile performance: reduce distortion strength and disable heavy effects.

DELIVER:

Pages/routes: Home, Works (All + Focus), Work Detail, About, Contact.

Components: WorksFocusLensCarousel, WorksAllGrid, AboutChapters.

A theme/tokens file for typography, spacing, and easing curves.

The “implementation hint” that makes this actually work

Tell Replit explicitly to do the lens transition using a single WebGL plane that blends between 2 textures with a displacement shader.

If the Agent struggles, paste this follow-up instruction:

FOLLOW-UP

Implement the lens swipe as a WebGL shader transition:

Two textures: current and next image.

A circular mask centered at (cx, cy) with radius r.

Outside mask: vignette + subtle blur simulation + stronger displacement.

Use a noise/displacement texture; during drag, uniform progress follows drag distance; on release, tween progress to 1 or back to 0 via GSAP.

When progress hits 1, swap textures and reset progress.